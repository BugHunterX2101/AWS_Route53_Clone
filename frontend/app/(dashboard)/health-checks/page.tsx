import { ComingSoonPage } from "@/components/common/ComingSoonPage";

export const metadata = { title: "Health Checks | Route53 Console" };
export default function HealthChecksPage() {
  return <ComingSoonPage feature="Health Checks" description="Monitor the health of your resources and configure failover routing based on endpoint health status." />;
}
