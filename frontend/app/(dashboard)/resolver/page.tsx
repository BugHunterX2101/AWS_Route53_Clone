import { ComingSoonPage } from "@/components/common/ComingSoonPage";

export const metadata = { title: "Resolver | Route53 Console" };
export default function ResolverPage() {
  return <ComingSoonPage feature="Route53 Resolver" description="Configure DNS query forwarding rules between your VPCs and on-premises networks." />;
}
